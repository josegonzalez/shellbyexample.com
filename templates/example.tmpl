<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Shell by Example: {{.Name}}</title>
    <link rel="stylesheet" href="site.css">
</head>
<body>
    <div class="example" id="{{.ID}}">
        <h2>
            <a href="index.html">Shell by Example</a>: {{.Name}}
            {{if .IsBash}}<span class="label label-bash">Bash</span>{{else if .HasBashMix}}<span class="label label-posix">POSIX</span> <span class="label label-bash">+ Bash</span>{{else}}<span class="label label-posix">POSIX</span>{{end}}
        </h2>

        <table>
            {{range $index, $sub := .SubExamples}}
            <tr{{if $sub.IsBash}} class="bash-section"{{end}}>
                <td class="docs">
                    {{if $sub.IsBash}}<span class="label label-bash">Bash</span>{{end}}
                    {{$sub.Docs}}
                </td>
                <td class="code">
                    <div class="code-header">
                        <a class="edit-link" href="https://github.com/josegonzalez/shellbyexample.com/edit/main/examples/{{$.ID}}/{{$sub.Filename}}" target="_blank" rel="noopener noreferrer" title="Edit on GitHub">Edit</a>
                        <button class="copy-btn" data-target="code-{{$index}}" title="Copy to clipboard">Copy</button>
                    </div>
                    <pre id="code-{{$index}}" data-code="{{$sub.CodeText}}">{{$sub.Code}}</pre>
                </td>
            </tr>
            {{if $sub.OutputText}}
            <tr class="output-row{{if $sub.IsBash}} bash-section{{end}}">
                <td class="docs"></td>
                <td class="code output">
                    <div class="output-label">Output:</div>
                    <pre>{{$sub.OutputText}}</pre>
                </td>
            </tr>
            {{end}}
            <tr class="separator"><td colspan="2"><hr></td></tr>
            {{end}}
        </table>

        <p class="next">
            {{if .Prev}}<a href="{{.Prev.ID}}.html">&laquo; {{.Prev.Name}}</a>{{end}}
            {{if and .Prev .Next}} | {{end}}
            {{if .Next}}<a href="{{.Next.ID}}.html">{{.Next.Name}} &raquo;</a>{{end}}
        </p>

        <p class="footer">
            by <a href="https://josediazgonzalez.com">Jose Diaz-Gonzalez</a>
            | inspired by <a href="https://gobyexample.com">Go by Example</a>
            | <a href="https://github.com/josegonzalez/shellbyexample.com">source</a>
        </p>
    </div>
    <script src="clipboard.js"></script>
</body>
</html>
